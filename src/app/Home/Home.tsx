import { Typography, Box } from "@mui/material";

const Home = () => {
  return (
    <Box>
      <Typography variant="h4">Welcome to Home</Typography>
      <Typography>Content for the home page goes here.</Typography>
    </Box>
  );
};

export default Home;
